// import ErrandController from '../../src/controllers/errand';
// import { ErrandService } from '../../src/services/errand';
// import { CacheRepository } from '../../src/database/repositories';

// jest.mock('../../src/database/repositories/errand.ts'); //simular uma chamada para o destino
// jest.mock('../../src/database/repositories');
// //SUT -> System under test = Qual sistema vai ser testado

// const sut = () => {
//     const service = new ErrandService();
//     const cache = new CacheRepository();

//     new ErrandController(service, cache);
// };

// describe('Errand Controller', () => {
//     beforeEach(() => {
//         jest.resetAllMocks();
//     });
// });
